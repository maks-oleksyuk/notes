# Version control
.git
.github
.gitignore
.gitattributes

# Dependencies (installed during build)
vendor
node_modules

# Docker & deploy config
.ddev
.docker
!.docker/dev/php/entrypoint.d
.dokploy
.devcontainer

# IDE & editor
.idea
.vscode
.editorconfig

# QA & lint tooling config
config/utils

# Frontend tooling (no in-image asset build)
package.json
pnpm-lock.yaml

# Runtime & cache
var
bootstrap/cache/*.php
bootstrap/cache/filament
!bootstrap/cache/.gitignore

# Environment & certificates (personal)
.env*
!.env.example

# Asset build artifacts
public/build/

# Tests
tests/

# AI tooling
.claude
.mcp.json
boost.json

# Docs & misc
*.md
local

# OS files
.DS_Store
