// Normally loaded by src/app/layout.tsx — component tests mount pages
// directly, skipping the root layout, so without this they render unstyled.
import '@mantine/core/styles.css';

import '@/app/globals.css';
